"use client"

import PlayerWaittingRoomView from "@/views/player/PlayerWaittingRoomView";

export default function TemplatePlayerWaitting() {

    return (
        <PlayerWaittingRoomView
            username="{username}"
            serieTitle="{Titre de la serie}"
        />
    )
}